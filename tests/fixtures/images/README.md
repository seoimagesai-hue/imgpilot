# Image validation fixtures

Tiny synthetic images generated by `npx tsx scripts/generate-image-fixtures.ts`.

No copyrighted or user photos. Used by `tests/image-validation.test.ts`.

Regenerate after cloning if missing:

```bash
npx tsx scripts/generate-image-fixtures.ts
```
